[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "imscriber"
version = "0.1"
dependencies = [
]

[project.scripts]
imscriber = "imscriber.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]

